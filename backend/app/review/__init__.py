"""合同审查主链路（包门面）。

一份合同从文件到报告的路径：解析 → 抽取 → 规则 → 检索 → 闸口 → 报告，
在线（图）与离线两条入口共用同一套实现。
| 模块 | 职责 |
| --- | --- |
| parser.py | 文件解析：Word / PDF / 扫描件 OCR，正文与条款切块 |
| extractor.py | 字段抽取：结构化结果 + 原文引证 + 关键字段双读 |
| rules/ | 规则引擎：字段级 / 文本级 / 品类基线 / 评级 |
| reviewer.py | 双审：独立盲审、与主审 diff、复核门 |
| graph.py | 审核图：节点编排、人工审批中断与恢复 |
| pipeline.py | 离线链路：一次跑完一条合同并成文报告 |

依赖方向单向：parser 与 rules 不依赖上层；graph 与 pipeline 才编排它们。
"""
